/target
**/*.rs.bk
.DS_Store
*.swp
.idea/
.vscode/
state.db
state.db-*
# Local-development daemon state — vaults, sockets, demo dirs
*.db
*.db-*
*.sock
demo/
onyx-state.db
onyx-hub.db
onyxd.sock
# Local agent state (Claude Code scheduling lock, etc.) — never project code.
.claude/
